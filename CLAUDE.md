# Sotto

@AGENTS.md

`AGENTS.md` is the single source of truth for how agents work here. Edit it, not this file, when a convention changes. Claude Code specifics: `/code-review` runs the two-axis review and `tastify` the design pass that `AGENTS.md` asks for, and when the `link_pull_request` tool is present, link every pull request you create or work on to the thread.
