# 67 — Match T3's native compaction behavior

Published: https://github.com/millZach/Sotto/issues/67

<!-- sotto-threads-workspace-draft:24 -->

## Phase

Phase 5 of 6. Tickets in this phase can run in parallel; start this ticket as soon as its own blockers are complete. Do not wait for unrelated tickets in an earlier phase.

[Workspace milestone](https://github.com/millZach/Sotto/milestone/1) · [This phase](https://github.com/millZach/Sotto/issues?q=is%3Aissue%20label%3Athreads-workspace%20label%3Athreads%3Aphase-5)

## What to build

Use the provider's normal compaction strategy and T3's context recommendation and controls.

## Acceptance criteria

- [ ] Offer native manual compaction only where supported, prevent duplicate in-flight requests and report actual completion/failure.
- [ ] Match the pinned proactive Claude predicate: at least 100,000 context tokens and at least 70 minutes since the last context update; suppress during running/pending states as T3 does.
- [ ] Match Keep full history dismissal per context snapshot and native Claude resume-return choices, including Don't ask again; do not claim the heuristic measures cache expiry.
- [ ] Preserve native auto-compaction defaults and explicit overrides; verify clock-controlled boundary cases and Codex/Claude/Grok capability differences without a custom summarizer.

## Blocked by

- #66 — Show live tokens, context and estimated thread cost

## Shared constraints

Build on the existing native-provider foundation and the approved Threads workspace specification. Preserve Sotto thread IDs, native provider bindings, scoped memory, explicit management authority and uncertain-delivery reconciliation. Use the inspected T3 reference at commit d1d15c67f4a5fb82fd8d5e01e5e3b288296789c3 as behavioral evidence; retain Sotto styling and the user's explicit differences. No cross-provider continuation, automatic chat-to-project orchestration, skill manager or custom compaction engine. Follow the repository's applicable design, review and platform instructions. A completed slice must include its own focused tests and rendered verification when it changes UI.

Implementation baseline: the existing native-provider work on branch work/thread-providers through commit 52f1f42. Reuse it instead of rebuilding adapters. Confirm that baseline is available before starting; the complete release also requires delivery of existing #24. Existing parent issues and unrelated Dictionary, history-import, hybrid-memory and voice-benchmark work retain their scope.
