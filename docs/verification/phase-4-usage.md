# Phase 4 usage (#66)

Amended September 23, 2026: the context and cost figures this note verified are no longer shown. The owner removed them from under the composer on Threads and on Chats while choosing the workspace and branch layout for #127; the pick is recorded in a comment on that issue. Sotto still records and prices usage as described below, and compaction's recommendation still reads it; nothing on screen shows the figures.

Target: existing Windows desktop Electron workspace, 1280/1600 wide and minimum 820×560, light/dark, pointer and keyboard. Preserve branding.

This is a compact reading of native work beside its composer; a user can see measured activity and an explicitly estimated cost without leaving the conversation.

Concepts: (1) quiet inline instrument row beside composition (chosen); (2) expanded status drawer; (3) transcript annotations. The inline row preserves transcript/composer hierarchy and avoids a dashboard.

Acceptance checks:
- [x] Native token/context/elapsed fields remain optional; missing values never become zero. Cache categories retain provider semantics.
- [x] Durable replay deduplication, resets and changed models preserve honest observed estimates and versioned rates.
- [x] UI uses existing ink/palette, secondary type ≥12px, wrapping at minimum size, focusable native disclosure for cost basis, no new decorative motion (live numerical state supplies feedback; reduced motion unaffected).
- [x] Render light/dark at desktop and minimum size; inspect native disclosure keyboard interaction and unknown/partial usage.
- [x] Focused boundary tests and typecheck pass (root owns final integrated checks).

Reference inspected before interface edits: `artifacts/phase-three-visual-fixes/editor-minimized-beside-page-1280-light.png` (existing quiet composer, generous transcript, muted controls). Pinned T3 `ClaudeAdapter.ts` token usage normalization and Codex adapter fixture show provider-specific cache semantics; Grok ACP support/extension does not expose comparable token counts. No T3 code copied.

The installed Grok 1.0.5 comparison subsequently revealed native `turn_completed.usage` not projected by the pinned T3 extension. Owned synthetic session `01a09bf4-226f-7743-8857-5dfd3c6f0e6f` reported 35,220 input, 309 output (232 reasoning included), 6,912 cache reads, 0 cache creation, and 6,935 ms API duration over two model calls. The adapter now reads these native turn totals from live/history events and deduplicates their native prompt identity durably. API time is labeled separately from elapsed turn duration. It does not misrepresent aggregated calls as current context or undocumented `costUsdTicks` as dollars. Exact `grok-4.6-build` API-equivalent rates are not verified, so its estimate remains unavailable. The added fixture passed after an initial red missing-handler result.

Rate sources retrieved 2026-09-13: [GPT-6 Astra](https://developers.openai.com/api/docs/models/gpt-6-astra), [GPT-5.4](https://developers.openai.com/api/docs/models/gpt-5.4), [Claude Sonnet 4.6](https://platform.claude.com/docs/en/models/sonnet-4-6/overview), [Claude Haiku 4.5](https://platform.claude.com/docs/en/models/haiku-4-5/overview), [xAI rates](https://docs.x.ai/developers/pricing). Estimates use standard API-equivalent text-token rates and exclude tools, subscription allowances and paid overages. Exact model matches only; missing pricing stays unavailable. Requests beyond known simple pricing tiers remain unpriced, as do unknown Claude cache-write TTLs and Astra cache writes when the client omits their counter.

Installed-client comparison (read-only, prior owned synthetic sessions; no new paid turns): Codex session `01a09be0-0bdd-7a01-a46c-1646467b1343`, model `gpt-6-astra`, reported 23,551 input, 13,056 cached, 0 cache-write, 10 output, total 23,561, context window 258,400. Its native log snake_case fields match pinned T3 App Server camelCase breakdown including optional cacheWriteInputTokens. Claude synthetic session `da1acce6-c3ed-4bb8-8962-221dcefbcb4d` reported `claude-haiku-4-5-20251001`: 9 uncached input, 23,699 cache reads, 269 cache writes (all 1h), 154 output. Repeated assistant records reused the same message usage; the ledger uses message identity to replace, never sum these repeats. No transcript text or credentials copied.

Rendered verification: `scripts/phase4-usage-capture.mjs` launched the built Windows Electron application with a new synthetic profile, injected known usage over the existing validated state bridge, and captured 1280×860, 1600×1000 and 820×560 in light/dark. `artifacts/phase-four-usage/usage-*.png` and `usage-basis-*.png` show the affected component. Inspected minimum dark and expanded light at desktop/minimum: native estimate disclosure responds to keyboard Enter, visible focus, no clipping, composer remains dominant. The row has four data purposes (tokens, context, elapsed, estimate); expanded explanation adds one billing-basis purpose, all required state/units. No repeated facts elsewhere in the affected row. It introduces no motion. Synthetic UI captures do not constitute native accounting fixture tests.

Existing `nativeProviderSkills` + `nativeProviderActivity`: 7/7 tests passed after integration. `nativeUsage.test.ts` + `threadUsage.test.tsx`: 10/10 pass at approved native usage output/persistence and rendered-state seams. Red→green regressions caught missing input hiding valid output and an ambiguous lower cumulative snapshot being charged twice. Ambiguous resets now preserve the prior cumulative high-water baseline without pricing the ambiguous request and mark the estimate partial. Review caught a later unseen historical snapshot being recharged after a lowered baseline; the durable regression now retains $0.008 across 1000/100 -> 2000/200 -> 500/50 -> restart -> 1500/150, and charges only the excess above the prior 2000/200 when 2500/250 arrives. Other fixtures cover cache TTLs, unknown/changed models, stable message/stream dedup across restart, installed Astra counters, and unavailable versus explicit zero. Native keyboard disclosure was exercised in Electron (jsdom does not implement summary Enter activation). New modules lint passed. Root owns integrated final typecheck and full suite.

Prompt editor cross-lane inspection: created/sent a synthetic personal conversation, generated a prompt, edited it, clicked Copy, and read back the exact edited text from Electron's clipboard. The initial browser Clipboard API failed in the actual app; root fixed it using bounded main-window clipboard IPC. Rebuilt and repeated successfully. Escape restores focus to Edit prompt. Light/dark captures at 1280 and 820 live beside usage captures.

Claude SDK packaging: official SDK 0.3.270 is a development input; builder copies only self-contained `sdk.mjs`, package metadata and upstream LICENSE/README into `resources/claude-sdk`. No unused native CLI binaries or peer dependency trees are shipped. The helper resolves the explicit resource path in packaged Electron. Standalone copied sdk.mjs successfully read nine messages and forked a copied prior synthetic native session under an isolated config home. Runtime/notices checks pass (173 components), as do eight existing runtime-preparation/release-notice tests. Packaged helper byte hashes are checked against pinned source by the release verifier; actual isolated Windows package verification passed at release/phase4-verification/win-unpacked. The packaged Sotto.exe in Electron run-as-node mode imported its own resource sdk.mjs, read nine messages, and forked the copied synthetic session in an isolated home. The full release verifier passed exact production/external inventories, SDK byte hashes, notices, provenance, SQLite/FTS5, real packaged node-pty, and normal app/worklet launch. Build SHA f0a46d86c18628a4bd0ace93cc7f5c6e3115fff7499c7dbdb40a32ca85770622; this intermediate package verifies SDK integration, while root owns the final integrated build after subsequent source fixes. Existing release/win-unpacked was preserved.
