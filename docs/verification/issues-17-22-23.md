# Tickets 17, 22 and 23: integration verification

Implemented and independently reviewed against starting main `d086c86c1b517613a71c160b75009d8ed43c2bd7`. Final reviewed implementation: `94b18afc1f903c3ad25e1a13832e1230e9db611c`. Verification performed September 11, 2026 on Windows. This report records local implementation; it does not claim a remote push, release or ticket closure.

## Delivered behavior

- **#17:** The coordinator retrieves relevant explicit preferences in thread, project, global order, abstains below a lexical relevance threshold, caps serialized memory context at 8,000 characters and 20 whole memories, and records exactly the included memory IDs. Preference retrieval cannot grant permission. Synthetic evaluation passed 22/22 cases; warm Windows Electron retrieval measured **10.12 ms p95** over 1,000 samples against 10,000 stored rows. [Retrieval contract, evaluation limits and raw evidence](../perf/2026-09-11-explicit-memory-retrieval.md).
- **#22:** Native Claude Code stream-json threads support creation, sending, streaming, questions, explicit permissions, cancellation, native resume, recovery and takeover detection. They reuse native subscription authentication and model discovery. [Protocol, live smoke and limitations](../research/issue-22-claude-native-verification.md).
- **#23:** Native Grok Build ACP threads provide the same shared lifecycle, including continuing native work across adapter disconnect through explicit leader mode. Connection checks CLI 1.0.5 and ACP 1. [Protocol, live smoke and limitations](../research/2026-09-11-issue-23-grok-acp-verification.md).
- Existing connection settings select T3 Code, Codex, Claude Code or Grok Build. Native providers receive native sign-in errors and do not require the T3 endpoint. Provider switching disconnects inactive adapters and replaces their catalog and selection state.

## Checks

| Check | Result |
| --- | --- |
| Final `npm run typecheck` | Passed |
| Final `npm run lint` | Passed |
| Final `npm run build` | Passed; existing lucide-react directive warnings |
| Final Grok contracts/failures, generic adapter contract, provider switching | 46 passed, 2 expected fake-provider skips, 4 files |
| Claude shared contract and safety regressions | 24 passed |
| Memory-focused suites | 286 passed across 11 files |
| Combined full Vitest run | 2,263 passed, 1 failed, 9 skipped; 131 passed files, 1 failed, 4 skipped |
| Failed Codex assertion rerun | Passed individually; full `codexHost.test.ts` then passed all 39 tests |
| Electron memory and provider selection tests | 3 passed |
| Extended provider selection test at 760 and 420 CSS pixels | Passed |
| Real Claude and Grok synthetic native flows | Passed as documented in the provider reports |
| Main checkout after restoring pre-existing local edits | Typecheck and 104 tests across the four affected composer/navigation/settings suites passed |

The full-suite failure was the existing Codex test “clears the durable coordinator outbox and draft after a late send acknowledgement without retrying.” Its default one-second poll still observed one durable outbox entry after the in-memory draft had cleared. The focused case and the complete 39-test file passed on rerun. This is a timing-sensitive observation, not a proven root cause or a claim that the full run was green. No Codex logic or assertion timeout was changed to suppress it.

The full suite ran before the final Grok live-status correction. The affected Grok/provider checks, typecheck, lint and build were rerun after that correction. No second full-suite run was needed for the localized change.

The implementation was fast-forwarded onto local `main`. All added and removed lines from the eight pre-existing modified files were compared with a saved patch and preserved exactly; those unrelated changes remain uncommitted. No push or ticket closure was performed.

Rendered Electron screenshots were inspected at desktop size and at 760/420 CSS pixels: provider labels, connection controls and dialog bounds were readable and contained. Tests also verified provider persistence after reload, native-field visibility, disabled selection while connected and re-enabled selection after disconnect. These UI tests use an isolated deterministic host; the separate native smoke tests exercise actual CLI protocols. The user's saved questionnaire responses and normal application profile were not used by these tests.

## Standards

Independent final review of `d086c86..94b18af`: **no unresolved findings**. The reviewer reproduced and verified the Grok status correction: running state survives delayed persistence, concurrent manual sends reject and live errors survive polling. Earlier Claude initialization, takeover-during-persistence and attachment-frame fixes remain intact. No remaining documented standards breaches or actionable heuristic smells were found.

## Spec

Independent final review: **no unresolved specification findings or scope creep**. The final Grok changes preserve running, completion and error events during history lag and satisfy the event, recovery and takeover requirements. The Claude IDE-context takeover correction remains intact. The review did not independently repeat the parent-owned suite or rendered UI checks.

Standards: 0 unresolved findings, no remaining severity. Spec: 0 unresolved findings, no remaining severity.

## Remaining boundaries

Memory evidence uses draft synthetic fixtures and an extractive reader; it is not an end-to-end model-quality benchmark. Lexical retrieval can miss paraphrases. Native permission and question shapes are tested with strict source-backed fakes; live smoke tests did not execute coding tools. Native takeover was tested with headless CLI resume, not interactive terminal UI. Foreign-session discovery, T3 removal and hybrid memory remain separate tickets. Grok images and existing-thread model changes are not advertised. Packaged application and macOS verification were not performed for this batch.
