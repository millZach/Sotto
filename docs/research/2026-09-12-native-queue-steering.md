# Native queue and steering evidence — 2026-09-12

Installed `codex --version`: `codex-cli 0.154.0`. Read-only `codex app-server generate-json-schema --out artifacts/queue-protocol` completed successfully; selected exact generated v2 request/response schemas are retained in `artifacts/queue-native/`. No model turn, account change or user project mutation was performed against installed clients.

Official primary reference fetched during implementation: [Codex App Server — steer an active turn](https://learn.chatgpt.com/docs/app-server#steer-an-active-turn), reached through https://developers.openai.com/codex/app-server. It establishes that `turn/steer` appends input to an active turn, requires `expectedTurnId`, fails without an active turn, emits no new turn-start event, and accepts no turn-level overrides. Installed schema confirms `threadId`, `expectedTurnId` and `input` are required; response is `turnId`. Skill input is part of installed UserInput; the concurrent skills lane validates selected native references before constructing it.

Pinned T3 reference: `d1d15c67f4a5fb82fd8d5e01e5e3b288296789c3`, source `apps/server/src/provider/Layers/CodexAdapter.ts:2511` and `codexSessionRuntime.ts:2433`. Its adapter delegates to the bound runtime; runtime sends `turn/start` and preserves an already active turn ID because the returned ID may describe queued work. Sotto's explicitly agreed difference is a local durable follow-up queue plus a distinct native steer command (`docs/threads-product-discussion.md`, lines 19 and 52). We do not reuse a queued turn ID as interruption or steering authority.

Implemented wire behavior: Codex uses the adapter's currently active native turn ID for `turn/steer`. Origin metadata is saved before dispatch; successful acknowledgement creates the exact Sotto message identity; uncertain acknowledgement retains origin and triggers read/reconciliation. Claude and Grok explicitly reject steering rather than cancel/resend. Their existing live completion messages now carry last-turn outcome into the queue gate. Grok additionally restores native completion from update history. Claude's historical transcript does not prove prior turn completion; queue recovery pauses for explicit review/resume when that evidence is absent.

Verification is process-backed fixture RPC verification, not a paid live native journey. Tests cover exact method/shape, same active turn, no extra start/cancel, lost acknowledgement without replay, native request waiting, unsupported providers, and queue dispatch after live Claude/Grok completion. Parent must run the integrated native Codex queue/skill/activity/steer journey with its coordinated temporary project.
