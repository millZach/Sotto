appId: com.talktype.desktop
productName: TalkType
copyright: Copyright (c) 2026 TalkType

directories:
  output: release

files:
  - out/**/*
  - package.json

asar: true

win:
  executableName: TalkType
  target:
    - nsis

artifactName: '${productName}-${version}-${arch}.${ext}'
